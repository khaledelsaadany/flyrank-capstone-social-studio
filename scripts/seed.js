const {getDatabase,resetDatabase}=require('../src/db/connection');const {postsRepo}=require('../src/db');const db=getDatabase();resetDatabase();const post=postsRepo.create({title:'Building Reliable Publishing Systems',author:'Khaled El Saadany',rawContent:'Reliable publishing systems need durable state, strict validation, human review, and idempotent retries. SQLite WAL mode provides a lightweight persistent foundation.'},db);console.log(`Seeded sample post: ${post.id}`);