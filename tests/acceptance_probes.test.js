const test=require('node:test');const assert=require('node:assert/strict');const {createApp}=require('../src/app');const {getDatabase}=require('../src/db/connection');test('health endpoint is available',async()=>{const app=createApp(getDatabase(':memory:'));const res=await require('supertest')(app).get('/api/health');assert.equal(res.status,200);assert.equal(res.body.status,'ok');});