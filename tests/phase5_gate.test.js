const test=require('node:test');const assert=require('node:assert/strict');const {DurableSchedulerWorker}=require('../src/scheduler/worker');test('worker exposes crash recovery and batch processing',()=>{const worker=new DurableSchedulerWorker(null,{pollIntervalMs:50,batchSize:2});assert.equal(typeof worker.recoverZombieJobs,'function');assert.equal(typeof worker.tick,'function');});