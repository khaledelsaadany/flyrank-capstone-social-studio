const crypto=require('crypto');const {SocialPublisher}=require('./base');class MockLinkedInPublisher extends SocialPublisher{constructor(){super('mock_linkedin','linkedin');this.recordedPosts=[];}async publish(payload){const {variantId,slotId,content,hashtags=[],idempotencyKey}=payload;const hash=crypto.createHash('sha256').update(idempotencyKey).digest('hex').slice(0,16),externalPostId=`urn:li:share:mock_${hash}`,externalUrl=`https://linkedin.com/feed/update/${externalPostId}`,formattedContent=`${content}\n\n${hashtags.join(' ')}`.trim(),preview=`[LinkedIn Professional Card Preview]\n${formattedContent}`;const record={success:true,adapterName:this.name,platform:this.platform,idempotencyKey,variantId,slotId,externalPostId,externalUrl,preview,payloadPreview:JSON.stringify({platform:'linkedin',visibility:'PUBLIC',author:'urn:li:person:mock_author',text:formattedContent,simulatedAt:new Date().toISOString()}),timestamp:new Date().toISOString()};this.recordedPosts.push(record);return record;}}module.exports={MockLinkedInPublisher};