# Evidence

The acceptance suite exercises the backend foundation and scheduler. The implementation is designed around six acceptance areas: ingestion and generation, constraint enforcement, review gating, adapter publishing, idempotent retries, and worker recovery.

Use `npm test` to execute the current automated checks.